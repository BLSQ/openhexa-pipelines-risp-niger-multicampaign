name: Push "build visualisation tables" pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/push-build-visualisation-tables.yml"
      - "build_visualisation_tables/**"
      - "shared/utils.py"
      - "scripts/sync_shared_utils.py"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Sync shared_utils.py from canonical source (shared/utils.py)
        run: python3 scripts/sync_shared_utils.py --check

      - uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Configure OpenHEXA CLI
        uses: blsq/openhexa-cli-action@v1
        with:
          workspace: "pev-niger-7cc1fb"
          token: ${{ secrets.OH_TOKEN }}

      - name: Push pipeline to OpenHEXA
        run: |
          openhexa pipelines push build_visualisation_tables \
            -c build-visualisation-tables \
            -n ${{ github.sha }} \
            -l "https://github.com/BLSQ/openhexa-pipelines-risp-niger-multicampaign/commit/${{ github.sha }}" \
            --yes
